//Sets
var items = new Set(["item2","item3",4]); //sets are unique by default. so no doublicates are allowed.
items.add("item1");

console.log(items);
console.log(items.size);
console.log(items.has("item1"));

items.clear(); //will empty every thing.


//weakSets : look at the video


























